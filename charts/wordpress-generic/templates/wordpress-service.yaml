apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-wordpress
  labels:
    app: {{ .Release.Name }}-wordpress
spec:
  type: ClusterIP

  selector:
    app: {{ .Release.Name }}-wordpress

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP